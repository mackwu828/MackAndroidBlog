<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.mackwu.activity">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".MainActivity" >
            <intent-filter>
            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>


        <!-- start -->
        <activity android:name=".start.StartAActivity" />
        <activity android:name=".start.StartBActivity" />
        <activity android:name=".start.StartProcessActivity" />
        <!-- launchMode -->
        <activity android:name=".launchmode.StandardActivity"
            android:launchMode="standard">
            <!--<intent-filter>-->
                <!--<action android:name="android.intent.action.MAIN" />-->

                <!--<category android:name="android.intent.category.LAUNCHER" />-->
            <!--</intent-filter>-->
        </activity>
        <activity android:name=".launchmode.SingleTopActivity"
            android:launchMode="singleTop"/>
        <activity android:name=".launchmode.SingleTaskActivity"
            android:launchMode="singleTask"/>
        <activity android:name=".launchmode.SingleInstanceActivity"
            android:launchMode="singleInstance"/>

    </application>

</manifest>