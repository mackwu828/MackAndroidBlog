<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.mackwu.service">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- service -->
        <service android:name=".start.StartService" />
        <service android:name=".start.BindService" />
        <service android:name=".foreground.ForegroundService" />
        <service android:name=".ipc.BookService" />
        <!--
            android:name="android.accessibilityservice.AccessibilityService" 声明辅助功能服务
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE" 权限
            android:label="xxx" 名称
            android:resource="@xml/accessibilty_config" 配置
        -->
        <service
            android:name=".accessibility.AccessService"
            android:label="accessibility test"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibilty_config"
                />
        </service>


        <!-- activity -->
        <activity android:name=".start.StartActivity" />
        <activity android:name=".start.BindActivity"/>

    </application>

</manifest>