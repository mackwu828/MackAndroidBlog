<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.mackwu.view">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />

    <application
        android:name=".ViewApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- base -->
        <activity android:name=".basic.TextViewActivity" />
        <activity android:name=".basic.EditTextActivity" />
        <activity android:name=".basic.SpanActivity" />
        <activity android:name=".basic.ActionBarActivity" />
        <activity android:name=".basic.RecyclerActivity" />
        <activity android:name=".basic.webview.WebViewActivity"/>
        <activity android:name=".opensource.X5Activity" />
        <activity android:name="com.mackwu.view.basic.viewpager.VpActivity" />
        <activity android:name="com.mackwu.view.basic.viewpager.VpFTabActivity" />
        <activity android:name=".basic.DialogActivity" />

        <!-- md -->
        <activity android:name="com.mackwu.view.basic.md.DrawerActivity" />
        <activity android:name="com.mackwu.view.basic.md.CardActivity" />

        <!--   open source     -->
        <activity android:name=".opensource.FlycoTabActivity" />

    </application>

</manifest>