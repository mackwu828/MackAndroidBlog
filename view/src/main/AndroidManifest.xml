<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.mackwu.view">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".MainActivity">
            <!--<intent-filter>-->
                <!--<action android:name="android.intent.action.MAIN" />-->

                <!--<category android:name="android.intent.category.LAUNCHER" />-->
            <!--</intent-filter>-->
        </activity>

        <!-- base -->
        <activity android:name=".TextViewActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".EditTextActivity" >

        </activity>
        <activity android:name=".SpanActivity" />
        <activity android:name=".ActionBarActivity" />
        <activity android:name=".vp.VpActivity" />
        <activity android:name=".vp.VpFTabActivity" />
        <activity android:name=".recycler.RecyclerActivity" />


        <!-- md -->
        <activity android:name=".md.DrawerActivity" />
        <activity android:name=".md.CardActivity" />


    </application>

</manifest>